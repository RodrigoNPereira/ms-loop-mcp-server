name: Release trigger test

on:
  release:
    types: [published]

jobs:
  probe:
    name: Confirm release event delivery
    runs-on: ubuntu-latest
    steps:
      - run: echo "release event reached GitHub Actions"
